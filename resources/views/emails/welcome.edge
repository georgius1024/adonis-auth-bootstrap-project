@layout('email')

@section('content')
    <h3> Здравствуйте, {{ user.username }} </h3>
    <p class="lead">Вы зарегистрировались на сайте <a href="{{siteUrl}}">{{siteName}}</a></p>
    <p class="callout">
        Для активации учетной записи, щелкните по ссылке <a href="{{ actionUrl }}" class="btn-primary"> Подтвердить e-mail</a>
    </p>
@endsection